<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ThemeOverlay.search" parent="Base.Theme.CRUDApp">

        <!-- Text color -->
        <item name="android:editTextColor">@color/white</item>
        <!-- Hint text color -->
        <item name="android:textColorHint">@color/black</item>

    </style>
</resources>